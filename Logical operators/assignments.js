const country = "Canada";
const population = 38.25;
const language = "English";

if (language === 'English'&& 50 > population ) {
  console.log(`YOu should live in ${country}`)
} else {
  console.log(`${country} does not meat your criteria :(`)
}